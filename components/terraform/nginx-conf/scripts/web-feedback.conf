# Web Feedback Locations

location = /feedback {
    rewrite ^([^.]*[^/])$ $1/ permanent;
}

location ~* ^/feedback/(.*)$ {
    proxy_set_header Host $host;
    proxy_intercept_errors off;
    proxy_set_header x-target "web-feedback";
    proxy_pass $ups_web_feedback/feedback/$1$is_args$args;
}
