resource "aws_cloudfront_trust_store" "worldpay" {
  count = var.environment == "live" ? 1 : 0

  name = "worldpay-sectigo-ca"

  ca_certificates_bundle_source {
    s3 {
      bucket = "ds-live-deployment-source"
      key    = "cloudfront/worldpay-sectigo-ca.pem"
    }
  }
}
